---
title: 變換秩：架構如何導航深度的頻譜病態 (Transforming Rank: How Architecture Navigates the Spectral Pathologies of Depth)
source: arxiv
source_url: https://arxiv.org/abs/2607.14018v1
pdf_path: /tmp/arxiv_papers/2607.14018v1.pdf
arxiv_id: 2607.14018v1
authors: [Katie Everett]
published: 2026-07-15
type: paper
tags: [論文, AI, arxiv]
sources: [https://arxiv.org/abs/2607.14018v1]
created: 2026-07-17
summary_lang: zh-tw
has_full_text: true
---

## 變換秩：架構如何導航深度的頻譜病態
英文標題：Transforming Rank: How Architecture Navigates the Spectral Pathologies of Depth

作者：Katie Everett
發布日期：2026-07-15
arXiv ID：2607.14018v1
PDF：有

## 英文摘要
This paper investigates how each component of the Transformer feedforward block architecture determines how much rank survives across depth at initialization. Skip connections and normalization, long understood as controlling magnitude, are reinterpreted as mechanisms for preserving gradient rank across depth. The matrix multiplications and nonlinear activations that make networks expressive also reduce rank. Skip connections trade off rank collapse against ensemble-like behavior, controlled by the relative scales of the branch and the skip. Skip connections route the gradient around the residual branch where rank is lost, rather than along long gradient paths that encourage layers to compose. Normalization placement controls this same tradeoff by setting the branch-to-skip ratio across depth, unifying the normalization placement and depth scaling literature—explaining why rank collapses for Post-Norm but plateaus for Pre-Norm. The two-matrix structure uses additional parameters to preserve representation or branch Jacobian rank. The width expansion between matrices keeps the branch Jacobian full rank, following a Marchenko-Pastur law. The initialization rank of the input-output Jacobian predicts which networks train on CIFAR-10.

## 繁體中文摘要
本文研究 Transformer 前饋區塊架構的每個組件如何決定在初始化時有多少秩能跨越深度存活。Skip connection 和正規化長期被理解為控制幅度，本文將其重新詮釋為跨深度保持梯度秩的機制。使網路具有表達能力的矩陣乘法與非線性激活也同時降低了秩。Skip connection 在秩塌縮與類集成行為之間進行權衡，由分支與跳過的相對尺度控制。Skip connection 將梯度繞過殘差分支（秩流失處）路由，而非沿著鼓勵層組合的長梯度路徑。正規化層的放置通過設定跨深度的分支與跳過比例控制同一權衡，統一了正規化放置與深度縮放文獻——解釋了為何 Post-Norm 的秩會塌縮而 Pre-Norm 則趨於平穩。雙矩陣結構使用額外參數保持表示或分支 Jacobian 秩。兩矩陣間的寬度擴展使分支 Jacobian 保持滿秩，遵循 Marchenko-Pastur 定律。輸入-輸出 Jacobian 的初始化秩能預測哪些網路能在 CIFAR-10 上訓練。

## 關鍵知識點
- Skip connection 與正規化重新詮釋為跨深度保持梯度秩的機制
- 矩陣乘法與非線性激活降低秩，與表達能力存在根本矛盾
- Skip connection 在秩塌縮與類集成行為間權衡，由分支/跳過比例控制
- 正規化放置決定分支-跳過比例：Post-Norm 秩塌縮，Pre-Norm 秩平穩
- 雙矩陣結構的寬度擴展遵循 Marchenko-Pastur 定律保持滿秩
- 輸入-輸出 Jacobian 的初始化秩可預測網路可訓練性

## 主要貢獻
1. 將架構設計重新框架為在秩塌縮、類集成行為與參數數量之間導航的內在權衡
2. 統一了正規化放置與深度縮放文獻，從秩的視角解釋 Post-Norm vs Pre-Norm 差異
3. 揭示了雙矩陣 FFN 結構中寬度擴展保持滿秩的 Marchenko-Pastur 定律關係
4. 證明初始化時的 Jacobian 秩可預測網路在 CIFAR-10 上的可訓練性
5. 為深度網路架構設計提供了基於頻譜分析的新理論基礎

## 原始摘要（English）
We investigate how each component of the Transformer feedforward block architecture design determines how much rank survives across depth at initialization. We reinterpret skip connections and normalization, long understood as controlling magnitude, as mechanisms for preserving gradient rank across depth, since the very matrix multiplications and nonlinear activations that make the network expressive also reduce the rank. We show that skip connections trade off rank collapse against ensemble-like behavior, controlled by the relative scales of the branch and the skip: skip connections route the gradient around the residual branch, where rank is lost, rather than along the long gradient paths that encourage the layers to compose. The placement of the normalization layer controls this same tradeoff by setting the branch-to-skip ratio across depth, unifying much of the normalization placement and depth scaling literature, in particular why rank collapses for Post-Norm but plateaus for Pre-Norm. Other aspects of the architecture, like the two-matrix structure that expands and contracts the width, use additional parameters to preserve the representation or branch Jacobian rank. The second matrix decorrelates a coherent mean spike that would grow across blocks with a single matrix and uncentered activation, preventing the residual representation from collapsing. The width expansion between the two matrices keeps the branch Jacobian full rank: applying the rank-reducing activation in this expanded space leaves enough directions to span the original, at a width that follows a Marchenko--Pastur law. The initialization rank of the input--output Jacobian predicts which networks train on CIFAR-10. Taken together, we recast architecture design for deep networks as navigating an intrinsic tradeoff among rank collapse, ensemble-like behavior, and parameter count.
