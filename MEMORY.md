# MEMORY.md - Jarvis 的長期記憶

> 參考三級優先級框架管理：P0（核心永不淘汰）、P1（90天）、P2（30天）

---

## 核心設定

- [P0][2026-03-08] 身份：Jarvis，風格是「精明的助理」，效率導向
- [P0][2026-06-21] **Vault 路徑（已更正）**：`~/Documents/Pigo_Obsidian/`（Obsidian vault，git repo，remote = `git@github.com:Pigowenhsiao/Pigo_Obsidian.git`）；**不要與 `~/Documents/Pigo/` 混淆**（Pigo 是無關的第三個獨立 repo）
- [P0][2026-06-21] **Agent 路徑**：`~/Documents/Agent/`（git repo，remote = `git@github.com:Pigowenhsiao/Agent.git`，**不要與 Pigo_Obsidian 混淆** — 兩者命名相似但內容完全獨立）
- [P0][2026-04-10] Agent 路徑：~/Documents/Agent（獨立的 git repo）
- [P0][2026-04-30] Repo clone 預設路徑：`~/Downloads/`（日後新 clone 的 repo 放這裡）
- [P2][2026-06-26 → 已結案] **沿用異常清理（06-26 早批次）**：MEMORY 自身包含 #7564 的「workspace 90 untracked + 3 M」是 06-21 快照，**已 stale**（現場 `git status --short` 顯示只有 3M+1??：MEMORY.md / tasks/index.md / cron-handover.md + 2026-06-10_News-Update.md，無 90 untracked）。neat-freak SKILL.md drift 已修（workspace 141→183 行，與 Agent HEAD 088c03e5 對齊）。**沿用異常 ① ② ③ ⑤ 結案**；④ hermes-backup.sh 待 Pigo 裁決
- [P2][2026-06-26 → 已結案] **沿用異常 ④ hermes-backup.sh**（#7564 早批次）：`~/.hermes/scripts/hermes-backup.sh` 不存在，neat-freak Phase 5 永久略過。**Pigo 06-26 08:06 裁決走 (a) 建 sh** → 已寫入 `~/.hermes/scripts/hermes-backup.sh`（3137 bytes，可執行）。`--dry` 顯示備份 MEMORY.md / AGENTS.md / IDENTITY.md / SOUL.md / TOOLS.md / USER.md / memory / skills，跳過 arxiv_papers / handoffs / tmp_* 等。**沿用異常 5 條全結案**
- [P1][2026-06-26] **Session 摘要 06-26 08:30 UTC**：Pigo 07:27 進場問 Hermes cron → 看見 workspace 5 條沿用異常（#7564 列出）→ 全結案。**Workspace 4 個 commits**：`2e0c761` (.gitignore)、`ad40e3b` (沿用異常清理)、`051ecf4` (ingest 12 個 ??)、`3a34414` (④ 結案)。**Agent 1 個 commit**：`b873254ad` (frontmatter-corruption-bug.md)。**Hermes 1 個新檔**：`~/.hermes/scripts/hermes-backup.sh`（3137 bytes）。**未 push**。**次要發現**：(1) workspace remote = `https://github.com/Pigowenhsiao/workspace.git`，但本地 HEAD `f90b5fe7` ≠ remote HEAD `88f278ad`（diverged）；(2) Agent repo 有 18 個 auto-restore commits 在我工作期間背景同步（不是我做的）；(3) OpenClaw 框架每 session idle 自動觸發 image_generate 跑賽博朋克夜景（`~/.openclaw/media/tool-image-generation/image-1---afffcc96-...png`，mtime 05-22，UUID 一致 = 同一張 cache 圖重複附 6 次）。**未做**：Pigowenhsiao/Pigo-Agent 新分支（Cron 08:25 忽略）。
- [P1][2026-06-26] **Session 追加 06-26 13:08 UTC**：Pigo 12:41 進場問 aihot skill → 跑「過去 3 天全部」(mode=all, since=72h, take=100×3 翻頁, 共 300 條去重) → **Top 10 完整版直接寫進 vault**：`~/Documents/Pigo_Obsidian/00-Inbox/2026-06-26_aihot-top10-detail.md` (5284 bytes / 148 行, vault commit `9e7880a4`, **已 push**)。**教訓**：Pigo 13:06 罵「問題太多」→ 之前 5 分鐘內連給 6 個 Q 選項，**不該**這樣；**應該**自己找最佳解（寫 vault 一次到位）不繞 Q。**OpenClaw cron 刪除**：早上 08:43 Pigo 指示刪除 disabled 的「每30分鐘 vault + agent 同步任務」(id `4eefd549-...`)，已刪。**OpenClaw 升級檢查**：runtime 已是 `2026.6.8 (844f405)`（channel=stable, deps ok），無 pending update。**openclaw.json** `meta.lastTouchedVersion` 5.12 → 6.8（purely cosmetic）。**vault/agent 同步頻率**：從 30min（disabled 64 天）→ 改為 Weekly Defrag 每週一 04:00 台北。**image_generate 自動觸發**：未查源頭（08:15 Pigo 忽略）。**新工作未寫進 MEMORY 但已 commit 到 vault**：5 個沿用異常清理 + aihot top 10 + hermes-backup.sh。
- [P2][2026-06-26] **Agent repo PPT skills 索引（11 個）**：(Pigo 20:47 問) Agent repo `~/Documents/Agent/skills/` 內 ppt/presentation/slides 相關 11 個 skill。**主生成組 7 個**：`ppt-master` (393 行, 13 refs)、`ppt-master_v2` (底線, 393 行, 13 refs, 跟 master 差 3 處 name+觸發詞)、`ppt-master-v2` (連字號, 393 行, 跟 _v2 差 1 處 name)、`pigo-ppt-style` (Pigo 品牌 藍 #3B82F6/紫 #8B5CF6 + assets + agents)、`pptx-posters` (⚠️ 無 SKILL.md 只有 Readme+references, 未完成)、`media-documents/Slidedeck` (簡報大綱+Nano Banana Pro)、`media-documents/handdrawn-ppt` (手繪風, 21:9 cover/16:9 body, 繁中預設)。**工具組 3 個**：`document-skills/pptx` (Anthropic 官方)、`media-documents/pptx` (重複？)、`git-devops/scientific-slides` (學術 Beamer/PPT)。**dedupe 待裁決**：`ppt-master_v2` 跟 `ppt-master-v2` 99% 等價（references/ 全部 sha256 相同、SKILL.md 差 1 處 name），建議保留 `_v2` + 刪 `-v2`；`ppt-master` 已是被 v2 取代的舊版（也只差 name），建議刪。**未動**（Pigo 未明確授權刪除 Agent skill，SOUL 紅線）。
- [P2][2026-06-26] **Agent repo ppt-master 系列 dedupe 完成**：(Pigo 20:52 指示刪除_v2 + 舊的 master，20:55 同意 commit，20:57 同意 push + MEMORY) → 刪除 `ppt-master/` (13 refs + SKILL.md) + `ppt-master_v2/` (13 refs + SKILL.md + ~5500 icons + 5 workflows + scripts + templates)，**保留 `ppt-master-v2/`**（連字號版，**完整** 11637 icons + 5 workflows + 12 references + scripts + templates）。**Agent commit `f9639da78`** (23,992 個檔刪除, 375,928 行刪除) 已 push 到 `git@github.com:Pigowenhsiao/Agent.git` (ad7a9297f..f9639da78)。**教訓**：未事先查 size 就刪 — ppt-master_v2 比預期大很多（5500 icons 而非預期 393 行 SKILL.md），下次大刪前必須先 `du -sh`。
- [P0][2026-04-10] `/home/pigo/Documents/` 是受信任工作區；其中的 git 維護操作（含 pull、push、status、fetch、add、commit、rebase、checkout、branch）與必要的安全檔案操作，必須優先使用 `workdir + 單一命令 + security: full`；避免 `cd ... && ...`、`git -C ... 2>&1`、pipe 與 shell chain
- [P0][2026-03-08] 偏好：用繁體中文與 Pigo 溝通
- [P0][2026-03-08] 安全紅線：不主動發送未經確認的外部訊息
- [P0][2026-04-30] 工作流程：收到任務時優先順序——已安裝 skills → Agent repo Skill Index → 無則自己建議執行
- [P0][2026-03-09] 已安裝技能：agent-browser, multi-search-engine, self-improving, skill-vetter, openclaw-tavily-search, neat-freak (以及內建的 summarize, gog)
- [P1][2026-06-18] Cron 排程：5 個 jobs — HF Daily Papers 09:00 / neat-freak 每 12h (04:00+16:00±5min) / 每日新聞 00:00 (全天總覽) + 12:00 午間 (市場焦點，2026-06-18 新建 job 8d465121) / Weekly Vault Defrag 每週一 04:00
- [P0][2026-05-05] 系統訊息（System 回應、Heartbeat、Gateway 重啟通知等）一律使用繁體中文，不得夾雜英文或簡體中文
- [P0][2026-04-29] Telegram 群組已設定 `requireMention: true`，只有被 @ 提問才回應
- [P0][2026-06-19] **minimax Code Plan API key**：`sk-cp-` 開頭的 key 是訂閱制，**不會過期**（除非配額/帳號問題）。sync 來源：`~/.local/share/opencode/auth.json` 的 `minimax.key`。x-note 等 skill 讀 `~/.pi/agent/auth.json`，**記得同步過去**，否則會 HTTP 401（2026-06-19 x-note 批次踩過）。opencode 與 `.pi/agent` 是兩個獨立位置，任一個被清空都不會通知對方

---

## 回覆規則

- 一律使用繁體中文回覆
- 回覆精簡，只保留必要資訊
- 成功時只回簡短結論；失敗、需要決策或有風險時才補充原因
- 預設格式：結論 → 必要細節 → 下一步

- [P2][2026-07-08] **finance loop parser + reviewer 修復（2317/2303 報價錯誤）**（Pigo 04:09 UTC 進場，04:11 拍板「現在修」）：(a) **parser.py root cause**：`parse_body()` 用 NT$ regex 抓 TW 報價，當 body 含新聞 headline 如 `NT$15 Billion, Then Stock Retreat` 時，`RE_BODY_NT` 意外 match 到 `NT$15`，且因為「if NT$ matches exist 就只用 NT$ 不用裸 $」邏輯，蓋掉了真實 `$235.00` 報價。(b) **修法**：合併 NT$ + HK$ + 裸 $ 全部候選池，按 `(鄰居數_±5% × 2) + (出現次數 × 3)` 加權挑最佳；RANGE_TWD `(1, 5000)` → `(5, 6000)` 容納 MediaTek ~4000。(c) **reviewer 盲點**：`PRICE_RANGES["TWD"] = (1, 5000)` 沒擋 NT$15，且 change detection 只在有 LOOP-STATE baseline 時跑（2317/2303 不在 baseline → silently skip）。修法：新增 `KNOWN_TICKER_BANDS` dict（全 31 ticker 帶寬）+ 新 Check 3b，超帶寬就 SUSPECT 並明講「可能 parser 抓錯（新聞 headline、隔壁 ticker）」。**驗證**：重抓 31 ticker → 30 OK + 0 SUSPECT + 1 ERROR（2454 transient timeout，retry OK）。**NFLX 帶寬從 `(200, 1000)` → `(50, 200)`**（2026 年 NFLX 真跌到 ~$76，舊帶寬是錯的）。**未做**：fetch_batch.py retry 機制（單 ticker timeout 偶發）、parser title 改用 `og:price:amount` meta、Playwright selector 直抓 `data-last-price`。**檔案**：`~/.openclaw/workspace/skills/google-finance/lib/parser.py` + `~/.openclaw/workspace/skills/reviewer-finance/scripts/review_report.py`，備份在 `/tmp/{parser,review_report}.py.bak.1783484170`。**vault note**：`~/Documents/Pigo_Obsidian/00-Inbox/2026-07-08_finance-parser-reviewer-fix.md`（5646 bytes）。**SOP 強化**：看到「parser 報出某 ticker 異常價」先驗證 body 是否含新聞 headline，再懷疑 data source；reviewer 對無 baseline 的 ticker 一定要有 per-ticker 帶寬檢查，不能只靠 general sanity range。

- [P1][2026-07-08] **Finance Loop Completion Criteria SOP**（Pigo 23:42 拍板「現在開始做」，導入 Anthropic 「Getting Started with Loops」框架）：(a) **LOOP-STATE.md v1.1** 加「Loop Completion Criteria」段（8 條 checkbox：覆蓋率/帶寬合規/Baseline 可比/產出檔案/異常告警/狀態更新/Baseline 完備/錯誤率上限），對齊 Anthropic 「在跑 loop 之前談判『什麼叫完成』」精神。(b) **vault index note**：新開 `~/Documents/Pigo_Obsidian/08-Learning/Projects/quant-finance/loop-completion-criteria.md`（6472 bytes）集中放引用地圖 + 對齊矩陣 + roadmap。(c) **互相引用**：step 1 引用兩個 vault note → 兩 note 真實存在 → 驗證可達。**修法中跳的坑**：(1) 原 vault note `2026-07-08_finance-parser-reviewer-fix.md` 在 vault 數小時後遺失（可能是 tidy 或 git 誤刪）→ 從 session 內容重建（5898 bytes）；(2) subagent 點出 LOOP-STATE.md 的「變更日誌」段還沒追加 v1.1 紀錄 → 補上。**未做（Step 4）**：把 criteria 從文件變成 fetch_batch.py / review_report.py 的程式化 gate（reviewer exit code 加 ERROR 計數、write_report.py 拒絕在 criteria 任一 FAIL 時寫入）。預估 2-4 小時，待 Pigo 裁決。**關鍵 SOP**：(1) 看到「parser 報出某 ticker 異常價」先驗證 body 是否含新聞 headline，再懷疑 data source；(2) reviewer 對無 baseline 的 ticker 一定要有 per-ticker 帶寬檢查，不能只靠 general sanity range；(3) **loop engineering 不是工具，是 commit 標準** — 在跑之前就談判好「什麼叫完成」。

## 當前階段

- [P1][2026-04-29] llm-wiki 預設目的地改為 `00-Inbox/`，完成後再移至正式分類（Learning/articles/ 等）
- [P1][2026-05-03] 新筆記落地規則（強制）：所有新筆記一律先寫入 `00-Inbox/`，完成整理分類後再搬至正式位置（`08-Learning/` 各子目錄或 `09-Article-Notes/`）；此規則已寫入 `llm-wiki` 與 `note-update` 兩個 skill 的 SKILL.md
- [P1][2026-05-03] 任務日誌機制：建立 `memory/tasks/` 目錄，index.md 為首頁，每月一個 `.md` 檔；完成任務後同步更新 `memory/tasks/YYYY-MM.md` 和 `memory/tasks/index.md` 的待處理队列
- [P1][2026-06-26][STALE_AFTER=7d → 2026-07-03] **RTK 累計效益**：4,979 cmds / 24.3M in / 22.0M out / **省 2.3M tokens（9.6%）**（從 06-26 04:00 的 4,818 / 23.5M / 21.4M / 9.1% 提升 +161 cmds / +0.8M / +0.6M / +0.5%）；平均 1.2s/cmd。**Daily（06-26）**：235 cmds / 1.3M in / 1.0M out / 231.4K saved (18.5%) / 1.7s（save% 回升，午後活躍）。**Weekly（06-22~06-28 截至 06-26 16:08）**：1,020 cmds / 4.8M in / 4.0M out / 857.2K saved (17.7%)。**Monthly（2026-06）**：4,978 cmds / 24.3M in / 22.0M out / 2.3M saved (9.6%)。**07-03 之前重跑 `rtk gain -a` 更新此條**。**SOP**：neat-freak 跑時自動跑 `rtk gain -a -H`，產出「RTK 效益」章節附在報告中（2026-06-24 Pigo 決定）
- [P1][2026-04-29] Telegram groupPolicy 改為 mention-only（groupPolicy 受保護無法修改，但 requireMention 已設為 true）
- [P0][2026-06-18] 當前模型：minimax/MiniMax-M3（openclaw.json primary = runtime，皆為 M3；**MEMORY 早期誤記 M2.7 已更正**；fallback M2.5；M2.7/M2.7-highspeed/M2.5 皆已定義但未啟用）；Node v22.22.3；openclaw-tavily-search 已替代 tavily-search
- [P1][2026-06-20] **x-note 7.7.x 已 release**（commit `ac7bea369`+`e1bc4d5a2`，github.com/Pigowenhsiao/Agent.git）: Flow B Step 2 改為 fxtwitter API（`fetch_fxtwitter.py`，零認證零 CDP）；preflight `verify_score_scales.py` 改 score 1-5 vs 0-50 漂移；score threshold 固定 ≥3（1-5）。**未修復問題**：xnote2_curator.call_minimax_curator 仍 ~51s/call（`max_tokens=4000`+thinking enabled），target 7.7.3
- [P1][2026-06-20] **x-note 7.7.3 已 release**（commit `4b677ddc3`+`71b42af77`，github.com/Pigowenhsiao/Agent.git）: curator 30s SIGALRM hard cap + 1 retry + disk cache at `~/.cache/x-note-curator/`（sha256 key，重跑 0s）；score_and_note gate score≥4 才走 curator（~70% 減少 LLM call，wall clock 16min→3.6min）；xnote2_curator system prompt 0-50 docstring→1-5 對齊；parse_response 加 1-5 range check + dist outlier bucket（修 SCORE=0 KeyError）。Preflight `verify_score_scales.py` 7/7 PASS。Hermes session 09:22 UTC 實戰驗證全管線跑通。
- [P1][2026-06-20] **x-note 7.7.4 已 release**（commit `6370fed84`+`65a6339d9`，github.com/Pigowenhsiao/Agent.git）: 品質閘門（quality gate）+ sources traceability + curator_status；curator payload 關閉 thinking budget（Hermes 09:22 UTC 實測 51s/call → 預期縮短）。**Hermes production 2026-06-20 batch 已 ingest** vault（commits 81a83527 + b3fefce0 + 6d6995be），00-Inbox 從 21 個回升至 39 個
- [P1][2026-06-22] **x-note 7.7.5 已 release**（commit `1a3b0e84a`，github.com/Pigowenhsiao/Agent.git，06-22 10:59 UTC）: API fallback chain 硬化 — (a) fxtwitter endpoint 修正為 `/twitter/status/{id}`（舊 `/status/{id}` 會回 empty body）；(b) vxtwitter endpoint 必須用純 numeric ID，**不可**用 `{handle}/status/{id}`（會 empty）；(c) fallback 順序固定為 fxtwitter → vxtwitter（pure ID）→ ADHX → Playwright+cookies，fxtwitter empty 時**不可跳過** vxtwitter
- [P1][2026-06-25] **x-note 7.8.0 已 release**（commit `7c1f3f998`，github.com/Pigowenhsiao/Agent.git，06-25 08:11 +0900 = 06-25 04:11 UTC）: **Score contract migration 1-5 → 0-10**（breaking change）— (a) keep threshold ≥6.5（原 ≥3）、curator threshold ≥8.0（原 ≥4）；(b) `score_and_note.py` 強制執行 `verify_score_scales.py` preflight；(c) legacy quality scorer 預設 safe-by-default，刪除需加 `--delete`；(d) Flow A 改為 FxTwitter 為主（`fetch_fxtwitter.py` + `https://api.fxtwitter.com/twitter/status/<id>?decode=true`），baoyu-fetch+CDP 降為 fallback（threads/articles/失敗恢復）。**未修復問題**：`xnote2_curator.call_minimax_curator` ~51s/call 是否受 0-10 影響待驗證。**注意**：1-5 → 0-10 是真正分制變更，與 06-20「0-50 撤回」事件**無關**（不是 revert），是契約升級
- [P2][2026-06-23] **Agent repo 06-21 ~ 06-23 未記錄 commits**（neat-freak 觀察）：`ef07e5b28` evolve(neat-freak) rewrite SKILL.md 為 operational runbook + phase checklist（06-21 17:05）；`bc9c7fe75` feat(skills) add punk-skills（punk-cover + punk-avatar 視覺生成，adrianpunk/Punk-Skill，06-21 02:06）；`aa91c61c8` merge notebooklm-bridge skill into main（05:13 不詳）；`13a8153eb` chore auto-evolve skills 2026-06-21；`a5b6149bb` weekly defrag sync 2026-06-21；`2c0ba2497` feat @sairahul1 加入 AI/開發者工具清單（06-23 00:43）；`825a346ad` refactor 移除長期無內容帳號，新增 shao__meng/undefinedKi/kunchenguid/Easycompany333（06-23 00:46）
- [P2][2026-06-25] **Agent repo 06-25 13:31 UTC commit `4e9251993` neat-freak SKILL.md frontmatter corruption fix**（neat-freak 觀察）：修 SKILL.md Phase 3 與 Phase 5，**新增兩種 Pattern 偵測** — Pattern A `name: hermes-cron-jobs`（sync 時被 hermes-cron-jobs 內容覆寫）；Pattern B 檔案以 `import json` 開頭（Python import 殘留，line 4 覆寫 description）。驗證從 `head -3` 改 `head -5`。**新增 hermes-backup 缺失防呆**：Phase 5 加 `ls ~/.hermes/scripts/hermes-backup.sh` 預檢，**不存在就略過 phase** 不再 panic。commit 引用 `references/frontmatter-corruption-bug.md` 但實際 references 沒有此檔（**reference 缺失**，待補建）
- [P2][2026-06-25] **`~/.hermes/scripts/hermes-backup.sh` 不存在**（neat-freak 觀察）：`~/.hermes/scripts/` 實際只有 `bb-browser-daemon-startup.sh` / `cloakbrowser_twitter_test.py` / `extract_codex_cookies.py` / `horizon-daily.sh` / `kanban-daily-cleanup.sh` / `markitdown-mcp-health.sh` / `news-fetch.py` / `obscura-serve.sh` + `__pycache__`。**結論**：hermes 備份機制從未設定，neat-freak Phase 5 須略過。4e9251993 commit 已加防呆，下次跑 cron 不會誤判
- [P2][2026-06-25 → 已關閉 2026-06-26] **`frontmatter-corruption-bug.md` reference 缺失**（neat-freak 觀察）：commit `4e9251993` 修 SKILL.md 時引用 `references/frontmatter-corruption-bug.md`，但 reference 原本 0 內容。**Pigo 06-26 07:40 裁決走 (a) 補建** → 已寫入 `~/Documents/Agent/skills/neat-freak/references/frontmatter-corruption-bug.md`（3388 bytes，涵蓋 Pattern A/B 觸發情境、偵測腳本、修復流程、事件時間線）。MEMORY 沿用異常 ⑤ 結案
- [P2][2026-06-26] **Agent repo 06-25 21:37 UTC commit `a4aea5c1a` add @eng_khairallah1 to AI/開發者工具**（neat-freak 觀察）：Pigowenhsiao 手動 commit，`name.md` AI/開發者工具清單新增 @eng_khairallah1（位置在 @sairahul1 後、@shao__meng 前）；與 `ece98f6e` KAW: 12個月AI Engineer路線圖 by eng_khairallah 同作者同時間，是「先 commit 清單 → 再 commit KAW 詳解」的雙步驟入庫流程。**該作者背景**：中東/埃及的 AI Engineer KOL（與 @sairahul1 同分類）
- [P2][2026-06-26] **Agent repo 06-26 07:56 + 08:05 UTC 兩個 commit 補完 neat-freak SKILL.md drift 與 frontmatter-corruption-bug.md**（neat-freak 觀察）：(a) `ad7a9297f` sync: neat-freak SKILL.md drift fix（+2/-1 行，與先前 06-26 12:00 早 session「drift 已修」觀察一致）；(b) `b873254ad` docs(neat-freak): add frontmatter-corruption-bug reference（3388 bytes / 145 行補完 commit 4e9251993 留下的 reference 缺失 — 但 workspace 副本仍未補建，仍是「雙版本 drift」的一部分）。**結論**：早 session 寫的「workspace 91→16 沿用異常結案」是 06-26 03:50 的 c2c0aa37 觀察，現在 16:08 跑時 00-Inbox 已回到 51（新聞+HF+mail-triage+KAW 累積），無需重新整理
- [P2][2026-06-26] **vault 06-26 16:05 UTC commit `b0d294bb` Add news update 2026-06-27**（neat-freak 觀察）：Hermes Agent cron「每日新聞 16:00」跑成功，7954 bytes / 89 行，commit author = `Hermes Agent <hermes@nos.com>`，vault remote 已 push（commit `c2c0aa37` 觀察的 bulk inbox maintenance 後續運作正常）。**與 06-22 觀察對比**：當時 8d465121 cron 觸發顯示「Apply Patch failed」但實際 commit 成功；本次無此誤報，可能是 06-22 後 OpenClaw cron registry 修過狀態上報
- [P2][2026-06-26] **Agent repo working tree 不 clean**（neat-freak 觀察）：3 個 modified（`memory/P0-core.md` + `memory/P1-90days.md` + `skills/x-note/x-note-playwright.py`），**全部源自 06-16 Pigo 手動 commit b176c96819981089 「memory path fix ~/Documents/ → ~/文件/」沒完成**（commit message 與 working tree 方向相反：commit message 寫「~/文件/」是 fix 方向，但 working tree 顯示「~/文件/ → ~/Documents/」回退）。**注意**：這是 Pigo 自己的事，不主動還原，等 Pigo 自己決定
- [P2][2026-06-26] **workspace repo 從 06-21 「90 untracked + 3 M」變 clean**（neat-freak 觀察）：本次 `git status --short` 顯示 0 個 untracked + 0 個 modified，**06-21 07:40 早 session 寫的「沿用異常 ① workspace 90 untracked + 3 M」已實際結案**。驗證當前 branch：`master`，無 upstream（仍 `fatal: 尚未給分支 'master' 設定上游`）。**結論**：MEMORY 06-26 早批次 [P2] 沿用異常清理已記錄為結案，這次再次驗證仍 clean
- [P2][2026-06-20] **0-50 分制訊息時間線**（避免下次重複繞路）: Pigo 06:34 訊息要求 0-50 評分→Hermes 跑 0 條（threshold 不匹配）→ Pigo 07:49 發現版次 7.7.2 是 1-5 → 撤回 0-50。OpenClaw session 訊息 08:29/08:36/08:42 是延續同一思考，最終「放棄」=保留 1-5 = 7.7.3 已做的方向。**未來若 Pigo 再要求 0-50，先確認是否真的要 revert**（影響 4 個 commit），別假設。
- [P2][2026-06-27] **hugohe3/ppt-master clone 放棄**（Pigo 06-27 12:04 台北 = 04:04 UTC 裁決）：21:35 cron `c446ff84-661f-493d-9a6d-b3dbb557cca1` 開 4 次失敗 timeout（consecutiveErrors: 4，120s timeout），Pigo **決定放棄更新 ppt-master**。cron 已刪除（deleteAfterRun: true 的一次性 job），`~/Downloads/ppt-master-hugohe3` 不建立。**現況**：`~/Downloads/GordenPPTSkill.zip` 仍在（Gorden 之前的 PPT skill）；`~/Documents/Agent/skills/ppt-master-v2/`（連字號版，11637 icons + 5 workflows + 12 refs）是**最新且保留的 PPT 來源**，不需再從 hugohe3 同步。**未來若要更新 PPT skill 庫，看 `ppt-master-v2/` 跟 `pigo-ppt-style/`（藍紫品牌）就好，別再繞 hugohe3**
- [P2][2026-06-27] **OpenClaw cron registry 對自管 agentTurn job 誤報 `Apply Patch failed`**（HF Daily Papers `b3eb6a9e` 2026-06-27 09:00 台北 = 01:00 UTC 實測）：consecutiveErrors: 1、`lastFailureNotificationDelivered: false`、diagnostic `⚠️ 🩹 Apply Patch failed`，**但 vault commit `f1ed54f6` + `dbc74100` 已 push 成功**（10 篇論文 + DanceOPD/PhysiFormer 完整筆記都進 `00-Inbox/`）。**這跟 06-22 午間新聞 cron `8d465121` 是同一個 bug**（見上方 [P1] 條目）。**SOP 強化**：看到 cron `Apply Patch failed` **先驗證** —— `git -C ~/Documents/Pigo_Obsidian log --oneline --since="<crontime>" | grep -i <job-keyword>` + `ls ~/Documents/Pigo_Obsidian/00-Inbox/ | grep -i <pattern>`，**不要信 registry status**。判定：commit 存在 + 檔案存在 = **實際成功，registry bug**；commit 缺失 + 檔案缺失 = 真失敗。**Hermes Agent 執行辨識**：commit author = `Hermes Agent <hermes@nos.com>` 或 `Hermes Cron <hermes@cron>`
- [P2][2026-06-27] **baoyu-skills 三件套升級完成**（Pigo 22:14 → 22:34 台北完成）：來源 `JimLiu/baoyu-skills`（monorepo，22 個 skill，新 layout = `skills/` 而非舊的 `.claude/skills/`）。**更新前版本** vs **更新後**：(a) `baoyu-url-to-markdown` 1.60.0→1.61.0、(b) `baoyu-article-illustrator` 1.57.0→1.117.4、(c) `baoyu-translate` 1.59.0→1.117.3。**更新方式**：`npx skills add JimLiu/baoyu-skills --skill <name> --agent openclaw --yes`（**分三次跑**—— `--skill` 逗號分隔**不被接受**，會回 `No matching skills found`，必須分開）。**網路慢**：每次 ~10-15 分鐘（`git clone --depth 1` 從 GitHub，無 sparse-checkout，~20MB tar）。**踩坑**：(1) `git archive --remote=` 被 GitHub SmartHTTPS 拒絕（2024 後 anonymous 不開放），改用 `codeload` tarball 或 `npx skills`；(2) `git ls-remote --tags` 看到的 v2.5.2 是**舊 layout**，main branch 才是 v1.117.x；(3) 直接抓 raw SKILL.md 覆蓋會壞 scripts/ + vendor/，**必須**走 `npx skills` 全套更新（會重建 bun.lock + 移除舊 vendor/、建立 scripts/lib/）。**Sandbox mirror**：`~/.openclaw/sandboxes/agent-main-f331f052/skills/` **不會自動同步**，需手動 `cp -r ~/.openclaw/workspace/skills/<name> <sandbox>/skills/`（OpenClaw 重啟才會自動重新鏡像，但期間 sandbox 跑舊版）。**備份**：原始 3 個目錄在 `/tmp/backup-baoyu-{name}-1782598894/`，要回滾直接 `cp -r` 回去。**未做**：smoke test（沒跑 `npx skills ... --use` 驗新 SKILL.md 能不能載入）、沒把 Agent repo 的 `baoyu-danger-x-to-markdown` 重新編譯（它早已是 v1.117.3 不用動）
- [P2][2026-06-27] **Agent repo ppt-master 系列 dedupe 完成**：(Pigo 20:52 指示刪除_v2 + 舊的 master，20:55 同意 commit，20:57 同意 push + MEMORY) → 刪除 `ppt-master/` (13 refs + SKILL.md) + `ppt-master_v2/` (13 refs + SKILL.md + ~5500 icons + 5 workflows + scripts + templates)，**保留 `ppt-master-v2/`**（連字號版，**完整** 11637 icons + 5 workflows + 12 references + scripts + templates）。**Agent commit `f9639da78`** (23,992 個檔刪除, 375,928 行刪除) 已 push 到 `git@github.com:Pigowenhsiao/Agent.git` (ad7a9297f..f9639da78)。**教訓**：未事先查 size 就刪 — ppt-master_v2 比預期大很多（5500 icons 而非預期 393 行 SKILL.md），下次大刪前必須先 `du -sh`
- [P1][2026-06-20] **OpenClaw session 跟 Hermes session 是兩個獨立 runtime**: OpenClaw 看到 `~/.openclaw/workspace/skills/` 跟 `~/.openclaw/workspace/MEMORY.md`；Hermes 看到 `~/.hermes/skills/` 跟 `~/.hermes/memory/`（含 `xurl` CLI、`~/.hermes/logs/agent.log`）。**OpenClaw session 不會看到 Hermes 那邊的訊息，反之亦然**。同一 Pigo 可能在兩個 session 都發訊息造成 race condition；要查 Hermes 進度可讀 `~/.hermes/logs/agent.log`（已驗證可行）。

---

## 臨時記錄

- [P2][2026-03-09] 技能安裝失敗歷史（已結案）：proactive-agent 頻率限制改由 self-improving 替代；tavily-search 改用 openclaw-tavily-search
- [P2][2026-06-21] **判斷錯誤案例（已結案，預防再犯）**：把 `~/Documents/Pigo/`（空殼）當成 vault、誤推斷 vault 沒有 git、又誤把 `Pigowenhsiao/Pigo_Obsidian` repo 內容當成 Agent。**教訓**：clone repo 或 ls 陌生目錄時，**先看 .obsidian/、AGENTS.md、index.md 等自我描述檔**，再下結論；不要只看目錄命名
- [P2][2026-06-21] **2026-06-14 vault defrag 報告是 defect**：cron 掃的是錯誤路徑 `/home/pigo/Documents/Pigo`（空殼）而非真 vault `~/Documents/Pigo_Obsidian`；**Weekly Vault Defrag cron 的 vault root 設定需要修正**（見 C 項）
- [P1][2026-06-22] **OpenClaw cron registry 對 Hermes-執行 job 誤報 `Apply Patch failed`**（2026-06-22 12:00 午間新聞 `8d465121` 實測）：OpenClaw 排程註冊 → Hermes 實際執行 → OpenClaw 收到的 status patch 解析失敗 → 顯示 error + Telegram 未送達。**但實際 vault commit `bf932ee9` 已 push 成功**。**教訓**：看到 cron `Apply Patch failed` **先查 vault git log 與檔案存在性**（`git -C <vault> log --oneline -5` + `ls <vault>/00-Inbox/YYYY-MM-DD_News-Update*.md`），**不要信 registry 的 status**。commit author 是 `Hermes Agent <hermes@nos.com>` 或 `Hermes Cron <hermes@cron>` 可作為 Hermes 執行的辨識特徵
- [P2][2026-06-22] **git log --since/--until 過窄會漏掉 cron commit**：每日新聞 cron 跑在 cron scheduled 04:00 CST（= UTC 20:00 前一天），commit timestamp 跟 cron 觸發時間有時差（+0~60 分鐘）；用 `git log --since=YYYY-MM-DD --until=YYYY-MM-DD+1` 之類的窄範圍會錯過。**正確查法**：`git log --all --oneline | grep <pattern>` 或 `git log --since=YYYY-MM-DD --author=Hermes` 寬一點
- [P1][2026-07-09] **OpenClaw cron registry `Apply Patch failed` 假警報模式定型**（Pigo 06:22 拍板「幫我處理修復假警報」→ 06:23 一個一個來）：第三次實測確認，HF Daily Papers `b3eb6a9e` 2026-07-09 09:00 台北 (= 01:00 UTC) registry 仍報 `Apply Patch failed` + Telegram 未送達，但 vault commit `3ff1be23` (Add HF papers 2026-07-09, 10 papers) + `7d81e631` (hf-daily: 2026-07-09) 都 push 成功，10 篇論文 mtime 01:10~01:11。**結論**：`Apply Patch failed` = registry 純假警報，**跟 06-22 午間新聞 `8d465121` + 06-27 HF `b3eb6a9e` 是同一個 bug，已成系統性模式**。**SOP**（已三次驗證，可信度 P1）：看到 `Apply Patch failed` 不處理，先查 (1) `git -C ~/Documents/Pigo_Obsidian log --all --oneline --since="<cron-scheduled-utc>" | grep <job-keyword>` + (2) `ls <vault>/00-Inbox/ | grep <pattern>`，兩個都有 = 實際成功，直接進 registry 把 error 清掉 / 不理它
- [P1][2026-07-09] **OpenClaw cron registry 假警報擴大為兩條**（驗證 neat-freak）：neat-freak daily sync `31f0439f-...` 2026-07-09 04:00 UTC 報 `⚠️ ⏰ Cron failed`（registry 標準假警報），但 agent 跑 177 秒（timeoutSeconds=3600 還有 57 分鐘沒用）、handover `memory/handoffs/neat-freak_2026-07-08-1600.md` 之前都成功寫入。**對比**：07-08 16:00 是 `⚠️ 🛠️ list files failed` warning（同樣的 registry 假警報但降級為 warn），07-09 04:00 升級為 `⚠️ ⏰ Cron failed` error。**SOP 升級**：除了 `Apply Patch failed` 外，`⚠️ ⏰ Cron failed` + `list files in ~/Documents/... failed` 都是 registry 假警報，**先查 vault/memory 是否有實際產出**：(1) `ls -la ~/.openclaw/workspace/memory/handoffs/neat-freak_*.md | tail -2`（看最近一份 handover mtime 是不是 04:00 UTC 後）+ (2) `git -C ~/Documents/Pigo_Obsidian log --all --oneline --since="<cron-scheduled-utc-12h>" | head -5`，handover 有 + vault 有新 commit = 實際成功。**這條比 HF 那條更危險**（容易誤判成路徑錯誤，但實際是 registry bug）
- [P1][2026-07-09] **Weekly Vault Defrag 真失敗模式定型**（`32b14abf-...`，Pigo 06:26 拍板修 2 走 A → 深查後改判）：連 3 週（06-22、06-29、07-06 週一 04:00 台北）0 defrag commit、vault 最新 defrag report = `12-Meta/vault-defrag-report-2026-06-28.md`、Agent repo 最新 = `e7661a959 Weekly defrag sync 2026-06-28`。**Root cause**：M3 + M2.5 + GLM-5.2 fallback chain 連 4 次「All models failed timeout」，durationMs 從 6.6s → 66s（**6.6 秒就 timeout 代表 agent 連 prompt 都沒解析就死**）。**這跟 HF/neat-freak 的 registry 假警報不同** — session 真的沒產出任何東西，handover 沒寫、commit 沒做、report 沒生成。**與 neat-freak 假警報的辨識點**：(a) defrag 失敗時 durationMs 都很短（< 70 秒） + (b) errorReason=timeout + (c) source=agent-run 不是 tool；(d) **vault / Agent repo 沒有對應 commit** 是最終判定。**已知對策**：(1) timeoutSeconds 已 3600 不用改；(2) 加 cron.retry policy 讓失敗後 30 分鐘自動重試（緩解單次 timeout）；(3) 換時段到週日 23:00 UTC 避開美國下午高負載（未驗證）；(4) **手動補跑**：若連 2 週失敗，手動 `cron run` 觸發當週一次
