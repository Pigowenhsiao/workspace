                                           Measurement-Driven Sub-Network Selection for
                                           On-Premise Retrieval-Augmented Factory Agents
                                                   1st Vasileios Rizeakos                        2nd Georgios Paisios                     3rd Alexandros Machairas
                                                  AI lab of enakronIC PC                  Electrical & Computer Engineering                AI lab of enakronIC PC
                                           Aspasias 72, 15561, Cholargos, Greece                   University of Patras             Aspasias 72, 15561, Cholargos, Greece
                                                    0000-0003-4475-6232                   Rio Campus, 26504, Patras, Greece                  0009-0009-7391-4659
                                                                                                  0009-0006-9713-3046

                                                4th Michael Birbas                                              5th Athanasios Bachoumis
                                        Electrical & Computer Engineering                                        AI lab of enakronIC PC
                                                 University of Patras                                     Aspasias 72, 15561, Cholargos, Greece
arXiv:2609.02760v1 [cs.AI] 2 Sep 2026




                                        Rio Campus, 26504, Patras, Greece                                          0000-0003-3887-9789
                                               0000-0002-6124-221X



                                           Abstract—On-premise assistants can give factory workers            alone deteriorates the general capability that reliable answering
                                        conversational access to machine documentation, but models            requires [6], [7].
                                        capable of the task rarely fit shop-floor hardware. We show that         Rather than treating the problem as a single compres-
                                        after structural compression and retrieval-grounded adaptation,
                                        model size is no longer a reliable predictor of adapted answer        sion step, this paper formulates it as a multi-stage pipeline.
                                        quality: general capability falls almost linearly with parameter      The approach combines a weight-shared supernetwork trained
                                        count, while judged retrieval-augmented answer quality does not.      through sandwich-style in-place distillation, a hardware-aware
                                        We therefore treat deployment as a post-adaptation selection          selection stage that identifies and commits to the most suitable
                                        problem, committing one sub-network per device on judged              sub-network for each deployment target, and a retrieval-
                                        answer quality and measured on-device throughput under a
                                        configurable general-capability floor and memory budget; rules        grounded distillation stage that further specializes the selected
                                        that optimize size, speed, or quality alone each give up capability   model using factory-specific documentation. Finally, the re-
                                        or throughput. A weight-shared supernetwork trained with              sulting model is integrated into a tool-augmented runtime for
                                        sandwich-style in-place distillation keeps this selection inexpen-    deployment and execution. Two empirical findings shape the
                                        sive. In a manufacturing-manual case study, extraction costs          design. First, cheap selection proxies are structurally uninfor-
                                        13.7% of the unpruned model’s judged quality and retrieval-
                                        grounded distillation returns it to within 4.6%, recovering two       mative on our candidate grids, where the natural quality proxy
                                        thirds of the loss, and the same assistant runs across three          is perfectly rank-correlated with parameter count. Second,
                                        heterogeneous edge tiers at 1.3 to 5 watts standby.                   a general-capability floor turns out to matter, disqualifying
                                           Index Terms—Large Language Models, Knowledge Distilla-             on both grids the very sub-networks a quality-throughput
                                        tion, Super-network, Model Compression, Retrieval-Augmented           blend would otherwise select. The central claim of this paper
                                        Generation, Edge AI, Smart Manufacturing
                                                                                                              follows: application adaptation can reorder compressed candi-
                                                                                                              dates, so deployment-time selection must run after adaptation,
                                                               I. I NTRODUCTION
                                                                                                              on measured application quality and device throughput under
                                           Much of the knowledge a factory worker needs at the                a general-capability constraint.
                                        machine lives in technical documentation consulted under time            Concretely, we ask whether a single weight-shared super-
                                        pressure. Large Language Models (LLMs) offer a natural-               network can provide sub-networks whose application-specific
                                        language interface to this material, and frameworks exist for         Retrieval-Augmented Generation (RAG) quality remains com-
                                        mapping LLM capabilities onto manufacturing tasks [2] and             petitive after structural compression (RQ1), whether hardware-
                                        for intent-based agentic automation [3]. Both presuppose a            measured selection under a general-capability constraint out-
                                        capable model at the point of use, which on a shop floor is           performs parameter-count or proxy-based selection (RQ2),
                                        an industrial controller or an embedded box whose documents           and whether the resulting pipeline can support practical
                                        and camera feeds may not leave the premises.                          on-premise deployment across heterogeneous edge hardware
                                           The obstacle is capacity versus footprint, since models that       (RQ3).
                                        answer technical questions reliably carry billions of parame-         This paper makes the following contributions:
                                        ters, while representative edge hardware offers two to eight             • Measured, capability-constrained selection: our central
                                        gigabytes of memory. Cloud offloading reintroduces latency,                 contribution, a hardware-grounded three-stage procedure
                                        recurring cost, and data-sovereignty concerns; training a small             (Sec. III-D) that commits one sub-network per deploy-
                                        model per deployment is uneconomical; and structural pruning                ment target by blending judged RAG quality with mea-
                                                                     A. TRAIN SHARED SUPERNETWORK
    sured on-device throughput under a configurable general-                                    instruction data
    capability floor, kept inexpensive by weight sharing and
                                                                         Llama-3.2
    a three-anchor throughput predictor.                                    base
                                                                                                                                                    11-candidate
  • Compression pipeline: A two-stage distillation pipeline                                                                                        calibrated grid
                                                                                                     weight-shared supernetwork
    for deployable LLMs: sandwich-style in-place distilla-                                             sandwich in-place KD

    tion of a weight-shared supernetwork with importance-
    calibrated sampling [1], followed by retrieval-grounded                  machine               retrieval-grounded KD (LoRA)
    distillation from the unpruned teacher into the extracted                manual                   teacher = unpruned base
    sub-network via low-rank adapters.
  • Case-study deployment and evaluation: A document-                B. ADAPT EVERY CANDIDATE
                                                                                                                  11 adapted candidates

    grounded, tool-augmented assistant instantiating the
                                                                       Q: RAG quality judged once (all 11)
    manufacturing-LLM framework of [2], measured across                                                                                            Pareto + floor
                                                                       T : • • • 3 anchors benched per device → predictor T̂ for all 11                Eq. (2)
    three edge tiers on judged quality, routing, latency, mem-
                                                                       C: ARC-Easy ≥ ϕ Cfull         M : memory ≤ budget
    ory, and energy under one protocol.                                                                                                   selected sub-network r ∗ per device

                     II. R ELATED W ORK                                             GPU tier                          Arm tier                     embedded tier
                                                                                    (Jetson)                          (RevPi)                        (Arduino)
A. Compressing LLMs
                                                                     C. SELECT PER DEVICE                                                                 edge device farm
   LLM inference cost is reduced by structural pruning with
recovery training [6], [7], knowledge distillation [8], [18], low-   Fig. 1. Train once, adapt every candidate, select per device. A weight-
rank adaptation [11], and quantization [22]. Weight-shared           shared supernetwork yields an 11-candidate calibrated grid; every candidate
                                                                     is adapted to the machine manual by retrieval-grounded distillation. The
super-networks train many sub-networks jointly under the             deployed rank is selected per device from judged quality and anchor-measured
sandwich rule [5], [17] and mature into train-once, specialize-      throughput under a capability floor and memory budget (Eq. 2).
per-device deployment [16]; hardware-aware benchmarks [19]
and on-device measurements [20] bring these ideas to LLM
scale. Train-once pipelines, however, select by pre-adaptation       Second, the extracted model is specialized to the target
accuracy predictors [16]; this paper shows selection must            application, with the unpruned teacher distilling retrieval-
follow application adaptation (Sec. III-D).                          grounded answers over the factory document into the sub-
                                                                     network through low-rank adapters (Sec. IV). The result is
B. RAG                                                               exported to a quantized format (8-bit ONNX for GPU targets,
   Retrieval-augmented generation grounds an LLM’s answer            4-bit GGUF for Arm CPUs) and serves as the reasoning core
in retrieved document passages rather than parametric memory         of the deployed agent (Sec. V).
alone [9]. Retrieval-aware finetuning (RAFT) [10] trains with
distractor passages mixed into the context; Sec. IV adopts this      A. Supernetwork and search space
inside a distillation objective.
                                                                        Supernetworks are built with an identical recipe at two
C. LLM agents in industrial scenarios                                scales, Llama-3.2-3B-Instruct and Llama-3.2-1B-Instruct [13],
                                                                     matching the deployment tiers of Sec. VIII; the 3B instance is
   In [3], intent-based agentic automation for manufacturing
                                                                     described here. The transformer layers are made elastic along
is proposed and [4] extends agentic orchestration to network
                                                                     two dimensions, network depth and per-layer MLP intermedi-
infrastructure and services, while in [2] an assistant framework
                                                                     ate size, which leave the shared residual dimensionality and
is presented, built around a task-orchestrator LLM delegating
                                                                     attention geometry intact (the costliest to recover after pruning
to external agents, explicitly including computer-vision mod-
                                                                     [6], [7]) and trade quality against size smoothly. Calibration
els. These frameworks generally assume large, typically cloud-
                                                                     collapses this space into an 11-point candidate grid spanning
hosted models, leaving a gap in how such capabilities can be
                                                                     2.20-3.21 billion unique (as-deployed) parameters on the 3B
realized through compact models inside resource-constrained
                                                                     base and 0.90-1.24 billion on the 1B; the largest candidate
industrial environments. This paper addresses that gap.
                                                                     is the unpruned base model itself. A candidate is activated
 III. S UPERNETWORK D ISTILLATION AND S UB - NETWORK                 in place by slicing the shared weight tensors, so no separate
                    E XTRACTION                                      student model is instantiated.
  The proposed pipeline (Fig. 1) compresses an instruction-
                                                                     B. Sandwich-style in-place distillation
tuned LLM in two distillation stages. First, the base model
becomes a weight-shared supernetwork finetuned with a                   The supernetwork is finetuned on general instruction data
sandwich-style in-place distillation rule, so that many candi-       (Alpaca-GPT4 [12]) before any sub-network is committed
date sub-networks are trained jointly (Sec. III-B). A compact        to. Following slimmable networks [5] and single-stage super-
sub-network is then extracted under a deployment budget              nets [17], each optimization step trains the full supernetwork
(Sec. III-C), and a hardware-grounded selection stage decides        together with M =3 sampled sub-networks, using the full
which candidate to commit per deployment target (Sec. III-D).        network’s detached output distribution as an in-place teacher.
Sampling is importance-calibrated, in that channels are pre-            new device, therefore, requires benchmarking only the three
permuted by importance and the M sub-networks are drawn                 anchors (6.0 hours instead of 28.8 for the full grid).
uniformly from the 11-point calibrated grid of Sec. III-C rather
than the raw space. The per-step objective is                                IV. R ETRIEVAL -G ROUNDED P OST-E XTRACTION
                                                                                             D ISTILLATION
                            M
                            X
                               αLCE (zi , y) + βτ 2 KL pτfull ∥ pτi ,
                                                                  
Lstep = LCE (zfull , y) +                                                  Distillation data: The application corpus is a 187-page
                            i=1                                         mill operator manual, split into 1,460 overlapping 100-token
                                                               (1)      chunks. Grounded question-answer pairs are first generated per
where zfull and zi are the logits of the full network and the i-th      chunk with grammar-constrained JSON decoding (686 training
sub-network on the same batch, y are the ground-truth tokens,           pairs plus a 633-question out-of-sample set). Each question is
pτ is the softmax softened at temperature τ , KL is the forward         then re-answered by the teacher, the unpruned base instruction
Kullback-Leibler (KL) divergence from the full network’s                model at the same scale, under the production prompt template
detached distribution, and α, β balance the terms (α=0.8,               and retriever top-4 contexts, so training matches inference
β=0.2, τ =1.0 in production). Gradients from all forwards               exactly. Following Retrieval-Aware Finetuning (RAFT) [10],
accumulate into the shared weights, transferring large-capacity         with probability 0.5 one or two of the four golden chunks
behavior to smaller candidates without a separate teacher pass.         are replaced by distractor chunks drawn from lower retrieval
The ablation in Sec. VII isolates what calibrated sampling              ranks, teaching the student to ignore near-miss context.
contributes.                                                               Objective: The extracted sub-network is adapted with low-
C. Sub-network extraction                                               rank adapters (LoRA) [11] of uniform rank r=8 on attention
                                                                        and MLP projections, keeping the base weights frozen. On
   After supernetwork training, sub-networks are materialized
                                                                        answer tokens only, the student minimizes
as standalone checkpoints. Candidates come from an 11-point
                                                                                                                     ′     ′
grid over the elastic dimensions, built by binning the parameter                     Lpost = α′ LCE + β ′ τ ′2 KL pτT ∥ pτS ,      (3)
range and keeping the best WikiText-perplexity configuration
per bin plus the smallest and the full one. Extraction itself is        with teacher distribution pT , student distribution pS , and
adopted from the sub-network selection literature [1].                  α′ =β ′ =0.5, τ ′ =2.0 in the deployed runs at both scales. Tool-
                                                                        routing demonstrations (Sec. V) are mixed into the same run
D. Hardware-grounded sub-network selection                              with the task loss only.
   Committing to one grid rank per target is itself a selection            Consolidation and export: After training, adapters are
problem, and cheap proxies are insufficient for it. The natural         merged into dense weights. The result is exported to ONNX
quality proxy (KL divergence to the full-width supernetwork             and reduced by post-training quantization [22] to blockwise
on production-shaped prompts) is perfectly rank-correlated              INT8 weights with FP16 activations (W8A16), the output
with parameter count (Spearman ρ=−1.0); no monotone                     head staying in full precision. For the Arm CPU tiers the
proxy can express the inversions of measured fronts. Selection          same merged checkpoint is instead exported to 4-bit GGUF
rests on measurement, in three stages: (i) proxy metrics for            (Q4 K M) for llama.cpp, under a verified parity contract tying
orientation, (ii) real evaluation, in which every rank is adapted       the input embeddings to the output head exactly as the ONNX
(Sec. IV), exported, and measured for routing accuracy, judged          build does. At these formats the 3B rank-6 occupies 1.85 GB
RAG quality, and on-device throughput, latency, memory, and             (Q4 K M) and 2.1 GB (W8A16); the 1B rank-5, 781 MB
energy (Sec. VIII), and (iii) selection, in which the deployed          (Q4 K M).
rank is
                                                                           V. AGENTIC R ETRIEVAL -AUGMENTED D EPLOYMENT
       r∗ = arg min w (1 − Q̃r ) + (1 − w) (1 − T̃r ) , (2)
                                                        
                 r∈F                                                       The compact model is the reasoning core of an on-premise
where Qr is judged RAG quality, Tr measured on-device                   assistant that adopts the task-orchestrator design of [2], in
throughput, tildes denote min-max normalization over the                which the orchestrator delegates to external agents (Fig. 2).
Pareto front, w=0.5, ties resolve toward higher throughput,                Each query is routed by the compact model itself, with
and F = {r : Cr ≥ ϕ Cfull , Mr ≤ Mmax } imposes                         grammar-constrained decoding guaranteeing valid JSON tool
an ARC-Easy capability floor (ϕ=0.8) and a memory ceil-                 calls, either to retrieval over the machine manual (dense
ing. The floor ϕ is a deployment-policy parameter, yet it               embeddings [14], top-4) or to a vision tool built on an off-
changes the outcome, disqualifying candidates retaining only            the-shelf person detector [15], then synthesizes the grounded
67-78% of the unpruned ARC-Easy score, among them the                   answer from the tool output under the distillation data’s prompt
unconstrained blend’s picks on both grids. A three-anchor               template (Sec. IV). Models are served with ONNX Runtime
throughput predictor (tok/s = a/nparams ), fitted per device on         GenAI (W8A16) on GPU targets and llama.cpp (Q4 K M)
three ranks spanning the parameter range, predicts the eight            on Arm CPU targets.
held-out ranks with 2.5-3.1% mean absolute error, and the                  The full stack runs on premises (development on a single
fronts computed from the predicted throughput coincide with             24-GB GPU partition), so documents and camera images never
the fully measured fronts on both grids. Commissioning a                leave the site.
 ON PREMISES
                                                                                                                     TABLE I
                                                                                          Q UALITY VS . MODEL SIZE AT THE DEPLOYED RANK ( GRID RANK 6;
        worker                 compact sub-network r ∗                      grounded   n=633 HELD - OUT QUESTIONS , ONE JUDGE PASS ). Q UALITY IS THE MEAN
        query                    router + synthesis                          answer
                                                                                        OF THE THREE RAG METRICS ; ∆ IS RELATIVE TO THE UNPRUNED BASE
                                                                                                    MODEL UNDER THE IDENTICAL RAG STACK .
 grammar-constrained                                                    tool
    JSON tool call                                                     output
                                                                                         Model                     Params      Size     Quality       ∆
               document retriever                           vision tool                  Base 3B + RAG (W8A16)      3.21B    2.42 GB        0.848     ref.
          top-4 chunks, dense embed.                     person detection                Extracted, no Stage 2      2.77B     2.1 GB        0.732   −13.7%
                                                                                          + Stage-2 KD              2.77B     2.1 GB        0.809   −4.6%
                     machine manual                           camera
                 (1,460 indexed chunks)                        feed
                                                                                                                     TABLE II
                                                                                        S TAGE -2 DECOMPOSITION AT EXTRACTION RANK 4 (n=291 HELD - OUT
Fig. 2. On-premise tool-augmented RAG assistant, instantiating the task-                QUESTIONS , ONE JUDGE PASS ; A DIFFERENT CAMPAIGN FROM TABLE I,
orchestrator design of [2]. The selected sub-network r∗ routes each query               SO ABSOLUTE VALUES DIFFER ). Q UALITY IS THE MEAN OF THE THREE
through a grammar-constrained JSON tool call and then synthesizes the                   RAG METRICS . ROWS BELOW “E XTRACTED ” ARE RECIPE ABLATIONS ;
grounded answer from the tool output.                                                  “+” ROWS BUILD CUMULATIVELY, AND THE LAST ROW IS THE DEPLOYED
                                                                                                                      RECIPE .


                           VI. E XPERIMENTAL S ETUP                                         Configuration                          Quality    ∆ vs. base
                                                                                            Base (unpruned) + RAG                   0.791         ref.
   Models: the supernetworks are Llama-3.2-3B- and 1B-                                      Extracted, no adaptation                0.701       −11.4%
Instruct (identical recipe); the deployed sub-networks are                                   supervised FT, context-free QA         0.654       −17.3%
chosen per platform by Sec. III-D at the balanced quality                                    task loss on grounded data (α=0.95)    0.727       −8.1%
                                                                                             + KD (α=β=0.5, τ =2)                   0.760       −3.9%
weight (3B rank 6 on the Jetson and the RevPi; 1B rank                                       + RAFT distractors (ours, deployed)    0.765       −3.3%
5 on the UNO Q); the post-extraction teacher is the un-
pruned base at each scale. Data: the common-task stage uses
Alpaca-GPT4 [12]; the application stage uses 686 manual QA                             633 paired questions; its bootstrap 95% confidence interval
pairs with RAFT distractors and a 633-question held-out set                            is [−5.9%, −3.3%] of the base score. General capability and
(Sec. IV), plus n=40 routing queries. Metrics: RAG quality                             adapted task quality also decouple. ARC-Easy falls almost
is measured by the three RAG metrics, faithfulness, answer                             linearly with parameter count (R2 =0.93); judged RAG quality
relevancy, and context utilization, scored by an LLM judge                             is only loosely size-ordered (R2 =0.76), with rank 6 matching
(Claude Opus 4.8, medium reasoning effort) in one interleaved                          the two largest candidates. Selection exploits this decoupling,
single-session pass per campaign (Sec. VIII); efficiency by                            and the capability floor guards against it.
TTFT, decode TPS, peak memory, and energy per inference
and at standby; and tool routing by accuracy. Judged scores                            B. Stage-2 decomposition and baselines
are comparable only within a session; absolute values differ                              Table II isolates the Stage-2 ingredients (n=291, one judge
across Tables I, III and IV. The vision tool uses an off-the-                          pass): context-free supervised finetuning degrades the ex-
shelf detector and is reported as functional, not benchmarked.                         tracted model (0.654 vs. 0.701), grounded task loss reaches
Baselines: the unpruned base under the identical RAG stack,                            0.727, the softened distillation term adds three points, and
the unadapted extraction, supervised finetuning, and the KD-                           RAFT distractors complete the deployed recipe at 0.765.
recipe ladder of Table II. Hardware: all training runs on
the on-prem GPU partition of Sec. V; supernetwork training                             C. Efficiency trade-off and selection outcome
dominates cost (∼51 h 3B, 22.8 h 1B); each Stage-2 variant of                             Fig. 3 shows the decoupling and the measured selection
Table II is a ∼12-minute LoRA run (the whole ladder under                              plane; Table III compares selection rules on the RevPi grid.
one GPU-hour). Deployment is evaluated on the three edge                               Every simpler rule forfeits something: size- and speed-driven
platforms of Sec. VIII. Supplementary examples: qualitative                            picks retain only 69-74% of base capability and quality-
answers with per-question judge scores and the vision demo,                            only ignores throughput, while the constrained blend keeps
at https://enakronic.github.io/llm-assistant-supplement/.                              all three (RQ2). On the 1B grid the floor likewise removes
                       VII. R ESULTS AND D ISCUSSION                                   the unconstrained pick (rank 10) and selects rank 5. The
                                                                                       floor sweep (Table III, bottom) shows the picks move only
A. Capability vs. model size                                                           at extreme values. With the floor in place, the blend weight
   Table I separates what pruning costs from what distillation                         barely matters, selecting rank 6 at every w ∈ [0.15, 0.85] on
recovers. Under an identical retrieval stack and a single judge                        both 3B devices and rank 5 on the UNO Q (rank 8 at speed-
pass over all 633 held-out questions, extraction at the deployed                       leaning w).
rank 6 drops judged answer quality by 13.7% relative to the
base; the retrieval-grounded second stage returns the model to                         D. Grounded question answering and tool routing
within 4.6% of the unpruned model’s judged quality, recover-                             The deployed cores’ scores appear in Table IV. The Jetson
ing two thirds of the loss (RQ1). The gap is estimated over                            and RevPi cores (rank 6) reach 0.773 faithfulness, 0.845
